# !! 本文件必须保存为 UTF-8 with BOM !!
# pip 在 Windows 上用系统 ANSI 代码页（中文环境为 cp936）读取本文件，
# 只有检测到 BOM 时才改用 UTF-8。去掉 BOM 会让下面的中文注释直接导致
# UnicodeDecodeError，pip install 整体失败。
## 直接依赖：只列出本项目源码中实际 import 的发行版，版本取自已通过回归的运行环境。
#
# 锁定策略
#   - 本文件用 == 精确锁定直接依赖，保证功能行为可复现；
#   - requirements.lock.txt 是同一环境的完整 pip freeze（含全部传递依赖），用于构建可复现的部署镜像；
#   - 升级依赖时先改本文件，安装后重新生成 lock，并至少执行：
#       python -m compileall -q app.py machining_unified scripts
#       python scripts/check_databases.py
#       在页面上完成 STEP / 文字 / 图片三种查询与一次企业资料问答。
#
# 重新生成 lock：
#   .\.venv\Scripts\python.exe -m pip freeze --exclude-editable > requirements.lock.txt
#
# 传递依赖说明（不在此处声明，由上游包拉取）：
#   openai                   <- langchain-openai
#   langchain-text-splitters <- langchain-classic
#   tiktoken / huggingface-hub / tokenizers <- langchain-openai / langchain-huggingface

# Web UI
streamlit==1.61.1

# STEP/B-Rep 几何解析（OCP 命名空间由二进制绑定提供）
cadquery-ocp==7.9.3.1.1

# 向量库
chromadb==1.5.9
langchain-chroma==1.1.0

# LangChain 运行时：以下四个均在源码中直接 import
langchain-core==1.5.4
langchain-classic==1.0.8
langchain-community==0.4.2
langchain-huggingface==1.2.2

# DeepSeek 通过 OpenAI 兼容接口调用
langchain-openai==1.4.3

# 嵌入模型（BGE 中文文本 / CLIP 图文）
sentence-transformers==5.7.0

# 词法检索
rank-bm25==0.2.2

# 文档与资料解析
pypdf==6.15.0
openpyxl==3.1.5

# 基础库
numpy==2.5.2
pillow==12.3.0
python-dotenv==1.2.2

