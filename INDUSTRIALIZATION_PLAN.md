# 3D 机械检索系统 — 企业级 / 工业化实施计划

> 状态：规划文档（2026-08-17）
> 适用范围：将当前"试点放行"的模型检索系统（508 模型 / 双 Chroma 库 / Streamlit 单进程）
> 提升到企业生产系统（全员使用、万级零件、接 PLM、有安全合规要求）的完整路径。
> 本计划不改变检索算法主线（已达标），重心是安全、数据治理、可观测、部署形态四件事。

---

## 1. 现状基线（2026-08-17 实测快照）

### 1.1 数据

| 项 | 值 |
|---|---|
| CAD 目录 | 508 条（24 教学 + 484 装配拆解件），`part_id` 唯一 |
| 向量库 | `cad_semantic` 508 / `multimodal` 508（CLIP 纯几何八视角，内部粗召回用） |
| 拆解台账 | `decomposed_parts.json` 484 条，跨装配复用 138，来源装配 13 个 |
| 设计属性 | 仅 3/508（0.6%）有材料标注——业务价值核心未激活 |

### 1.2 质量（回归层全绿）

- 门禁 R1 自检索候选覆盖率 100%；R2 复用件事实边 138/138；R3 KG 磁盘缓存命中 0.019s；R4 图号置顶覆盖率 501/508（98.6%）。
- 质量层 27 项指标记录在案：同装配共现提升度平均 5.80x；文本三族（轴/板/箱体）3/3；STEP 生产路径自检索 30/30；图片自检索 6/6。
- 已知限制（有登记无阻塞）：属性查询零命中（基线）；图片检索新图 14~34s；KG 冷构建 55~90s（磁盘缓存兜底）。

### 1.3 工程基线

- 原子重建索引、只读完整性检查、JSON 结构化日志、异常收窄原则、三级磁盘缓存模式（进程→磁盘→重算）已统一。
- 试点放行状态：🟢（内网 + 白名单 ≤10 人 + 结果人工复核）。

---

## 2. 总体判断

**检索算法已达标（甚至超出同规模产品），工业化的差距在系统能力，不在算法。**

> 代码只占三成，系统能力占七成。先补齐"企业敢把核心零件库数据交给你的理由"，
> 再谈算法增强。四个一票否决级/高优先级缺口：安全与审计、数据治理、可靠性与可观测、部署形态。

---

## 3. 工业化验收标准（目标定义）

达成以下全部条件，视为"企业生产系统"：

1. 未授权用户无法访问；只读/编辑/管理员三级权限生效；可回答"谁在何时查过哪个零件"。
2. 零件数据资产化：schema 版本化、台账规则版本化、增量更新不停服、字段（体积/更新时间）完整、来源可追溯。
3. 运行可观测：健康检查、延迟/失败/缓存命中指标、告警、备份与恢复演练、故障注入测试。
4. 万级规模可运行：5000+ 零件压测报告在案，检索延迟与内存曲线有数据支撑。
5. 检索准确率可证明：人工黄金评测集 200+ 条 + nightly 全量档 + 质量指标时间序列。
6. 业务价值兑现：设计属性覆盖主力装配 ≥30%；近似件/重复件报告可用。

---

## 4. 差距与工作项

### 4.1 安全与审计（一票否决，第 1 优先级）

| # | 工作项 | 涉及 | 要点 | 验收 | 重建索引 |
|---|---|---|---|---|---|
| S1 | 部署层鉴权 | 反向代理 + `app.py` | 接 LDAP/SSO/OIDC；应用内读 `X-User` 头；不引入应用内密码体系 | 未登录无法访问；普通用户无管理面 | 否 |
| S2 | 查询审计日志 | `app.py` / 检索服务层 | 记录 user/时间/查询词/图号/结果数；导入/重建操作审计 | 可回答"上周谁查过 XC3-KJ-555" | 否 |
| S3 | 权限分级 | 部署层 + `scripts/` 入口 | 只读（检索+查看）/编辑（导入/拆解/重建）/管理员（配置+日志） | 只读账号无法触发导入脚本 | 否 |
| S4 | 数据保密 | 备份脚本 | 审计日志脱敏、备份加密 | 备份离线可解密可审计 | 否 |

### 4.2 数据治理（数据资产化）

| # | 工作项 | 涉及 | 要点 | 验收 | 重建索引 |
|---|---|---|---|---|---|
| D1 | 台账规则版本号落盘 | `decompose_assembly_step.py`、`data/catalogs/decomposed_parts.json` | `rule_version` 写入台账；`check_databases` 校验存在 | 换机器重拆后数字可归因 | 否（下次拆解时） |
| D2 | 目录 schema 版本化 | `build_cad_catalog.py`、`cad_models.json` | `schema_version` 字段 + 迁移脚本（新增字段走迁移而非全量重建） | 加"体积"字段不动索引 | 否（迁移） |
| D3 | 增量索引管道 | `scripts/` 新增 | 新 STEP → 目录增量 → Chroma upsert | 加 10 个模型不停服 | 增量 |
| D4 | 体积/更新时间字段 | `cad/extraction.py`、`build_cad_catalog.py`、`ui/` | OCP `BRepGProp.VolumeProperties` 计算体积；目录构建写更新时间 | 结果卡片显示更新时间与体积 | 是（一次全量） |
| D5 | 设计属性数据工程（业务价值核心） | `data/catalogs/part_manifest.json`、`manifests.py` | 主力装配材料/表面处理/热处理/公差回填 | 属性覆盖 ≥30% 主力装配；门禁 Q2 属性查询转正 | 是 |
| D6 | 数据血缘 | `build_cad_catalog.py` | 目录记录 `imported_by/imported_at/assembly_of` | 可追溯每个零件来源 | 是（随下次重建） |

### 4.3 可靠性与可观测

| # | 工作项 | 涉及 | 要点 | 验收 | 重建索引 |
|---|---|---|---|---|---|
| O1 | 指标与告警 | 新增 metrics 模块 / Prometheus | 查询延迟 p95、失败率、缓存命中率、索引陈旧天数 | 延迟 p95>3s 告警 | 否 |
| O2 | 健康检查 | `app.py` 或服务层 | `/healthz`：目录+两库+缓存可读 | 探活 5s 内响应 | 否 |
| O3 | 备份与恢复 | `scripts/` 新增 | sqlite + 目录 + 台账备份脚本；季度恢复演练 | 演练通过并留档 | 否 |
| O4 | DeepSeek 故障注入测试 | `tests/` 新增 | 模拟超时/5xx，断言检索结果不受影响（当前已加 `timeout=15, max_retries=1` + `explanation=None` 降级，未验证） | 故障注入用例全绿 | 否 |
| O5 | 服务化 | 新增 FastAPI 薄壳 | 包 `search_by_*`；图片检索 14-34s 查询走异步任务 | 10 并发文字查询 p95<3s | 否 |
| O6 | KG 后台异步构建 | `knowledge/engineering.py`、面板 | 冷构建 55-90s 不阻塞首访 | 首访不等待冷构建 | 否 |

### 4.4 性能与规模

| # | 工作项 | 涉及 | 要点 | 验收 | 重建索引 |
|---|---|---|---|---|---|
| P1 | 5000+ 零件压测报告 | 压测脚本 + 文档 | 目录扫描/KG 构图/文字检索延迟/内存曲线 | 报告 + 瓶颈清单 | 否 |
| P2 | 图片检索精排向量预计算 | `build_unified_index.py`、`cad/visual.py` | 索引期预计算精排向量，查询只编码 1 图 + 点积 | 新图查询 <2s | 是 |
| P3 | 大文件 deflection 调优（112s 根因） | `cad/viewer.py`/`visual.py` + `build_unified_index.py` | 与 multimodal 重建绑定执行，避免 CLIP 输入口径漂移 | 最大零件渲染进 10s | 是 |
| P4 | ANN 决策 | 按压测结论 | 目录 >5000 门槛（CLAUDE.md 守门注释已有）引入 faiss/hnswlib | 万级检索亚秒 | 是 |

### 4.5 质量体系收尾

| # | 工作项 | 要点 | 验收 |
|---|---|---|---|
| Q1 | 人工语义黄金集 | 200+ 条标注查询入槽（CI 质量层） | 门禁质量层引用黄金集 |
| Q2 | 真实图片语料 | 收集零件真实照片/截图 50+ 张 | 图片检索评测脱离"自渲染图"构造性测试 |
| Q3 | nightly 全量档 | gates `--full` 每晚跑 + 27 项指标时间序列看板 | 趋势看板可回溯 |
| Q4 | 浏览器兼容性 | Chrome/Edge/Firefox 冒烟 | 三浏览器基础功能通过 |

### 4.6 产品化（企业付费能力）

| # | 工作项 | 对标 | 前置 |
|---|---|---|---|
| M1 | 近似件/重复件报告 | PARTsolutions Part Analytics | 形状描述子（D2 后） |
| M2 | PLM/ERP 连接器 | Geolus 生态价值 | O5 服务化后 |
| M3 | 组合条件筛选（产品决策挂起项） | — | 需 D5 数据 |
| M4 | 断崖截断比例外置化（`_CLIFF_RATIO` 现为源码常量） | 运营可调 | 否 |

---

## 5. 三阶段实施计划

### 第 1 阶段：企业试点扩大前提（1~2 个月）

**目标**：企业敢把核心数据放进来；运行不再依赖个人电脑。

顺序与依赖：S1/S2 鉴权+审计（最先）→ D1/D2 版本化 → O3 备份 → O1/O2 指标+健康检查 → O4 故障注入 → D4 字段。

| 周 | 工作项 | 出口标准 |
|---|---|---|
| 1-2 | S1+S2 鉴权与审计 | 未授权不可访问；审计可查询 |
| 3 | D1+D2 版本化 | 台账/目录 schema 版本字段存在 |
| 4 | O3 备份 + 演练 | 恢复演练留档 |
| 5-6 | O1+O2 指标/健康检查 + O4 故障注入 | 告警可达；故障注入全绿 |
| 7-8 | D4 体积/更新时间（迁移方式）+ 浏览器三件套 | 结果卡片显示新字段 |

**出口条件**：S1-S4 完成、D1/D2/D4 完成、O1-O4 完成、`test_upload_flows` + gates 全绿。

### 第 2 阶段：规模化（2~4 个月）

**目标**：万级可运行；准确率可证明。

| 周 | 工作项 | 出口标准 |
|---|---|---|
| 1-3 | P1 压测报告 | 报告 + 瓶颈清单；据此裁决 P4 ANN |
| 4-6 | D3 增量索引管道 | 加模型不停服 |
| 7-9 | O5 服务化 + 并发验证 | 10 并发 p95<3s |
| 10-11 | Q1 黄金集 + Q3 nightly | 黄金集入 CI；趋势看板上线 |
| 12-13 | P2/P3 图片检索预计算 + deflection | 新图查询 <2s；最大零件渲染 <10s |
| 14-16 | D5 设计属性补齐主力装配 | 覆盖 ≥30%；Q2 属性查询转正 |

**出口条件**：压测报告在案、增量管道可用、服务化并发达标、黄金集+nightly 上线、属性覆盖达标。

### 第 3 阶段：价值兑现（4~6+ 个月）

| 周 | 工作项 | 出口标准 |
|---|---|---|
| 1-4 | M1 近似件/重复件报告 | 管理层可读的零件削减报表 |
| 5-8 | M2 PLM/ERP 连接器 + S3 权限分级落地 | 图号反查/BOM 校验可用 |
| 9-12 | M3 组合条件筛选（若产品决策为补功能） | 分类+尺寸+材料+日期筛选可用 |
| 持续 | Q2 真实图片语料扩充、D6 血缘完善 | — |

**出口条件**：M1/M2 可用；企业化验收标准（第 3 节）全部达成。

---

## 6. 决策挂起项（含裁决条件）

| 挂起项 | 裁决条件 | 当前建议 |
|---|---|---|
| 组合条件筛选（补功能 vs 移出验收清单） | 试点期工程师查询日志是否频繁出现"按材料/尺寸筛"诉求 | 试点了再定；倾向补（D5 数据到位后成本低） |
| ANN 索引引入 | P1 压测报告：目录 >5000 且全量扫描延迟不可接受 | 按报告裁决；守门注释"目录<5000 禁止引入"保留 |
| 大文件 deflection 调优 | 必须与 multimodal 索引重建绑定，避免 CLIP 输入口径漂移 | 第 2 阶段 P3 执行 |
| 结果列表"更新时间"字段 | 与 D4 一并走 schema 迁移（不临时改） | 第 1 阶段 D4 执行 |

---

## 7. 风险与外部依赖

| 依赖 | 说明 | 缺省影响 |
|---|---|---|
| BOM/工程图数据（D5） | 设计属性回填依赖企业资料，非代码可补 | 业务价值核心继续空转 |
| 5000+ 零件样本（P1） | 压测需要真实/合成大目录 | 万级结论停留在估算 |
| 真实零件图片（Q2） | 图片检索评测脱离构造性测试的前提 | 图片检索准确率无真实证据 |
| LDAP/SSO 环境（S1） | 需 IT 配合 | 鉴权只能停留在反代白名单 |
| GPU/内存（P2/P3） | 预计算与编码加速依赖算力 | 图片检索停留在 14-34s |

风险控制：每阶段出口条件先于下一步启动；所有"宣称但未验证"的能力（如 DeepSeek 降级、缓存失效）优先补故障注入测试。

---

## 8. 变更记录

| 日期 | 变更 | 责任人 |
|---|---|---|
| 2026-08-17 | 初稿：基于试点放行后的全面复测（508 模型、R1-R4 全绿、面板/超时修复已验证）编制 | QA 负责人 |
